[project]
name = "path-finder"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "gitingest==0.3.1",
    "mcp[cli]==1.21.1",
]
